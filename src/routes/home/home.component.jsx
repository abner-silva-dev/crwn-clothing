import { Fragment } from 'react';
import DirectoryContainer from '../../components/directory-/directory.component';

const Home = () => {
  return (
    <Fragment>
      <DirectoryContainer />
    </Fragment>
  );
};

export default Home;
